# Eval Baseline — 2026-07-23

Snapshot taken 2026-07-23 on `main` at `6748fcd`, with the eval set as built after
PR #9 (eval-infrastructure): 113 cases including the 95 Bay Club label-CSV events
from the full-video tracking run + sidecar re-key. Supersedes BASELINE-2026-07-20.md,
which predates the label-CSV axis (18 cases only).

The headline weak axis is missed bounces at rally scale — **71/109**, dominated by
wall hits (65). That is the detector-recall problem (RF-DETR retrain case), not a
judge/calibration problem; changes that don't touch detection should not move it.

Reproduce with:

```
.venv/bin/python eval_line_calls.py --eval-set eval_set/cases.jsonl
```

(Only rebuild first — `build_eval_set.py --runs-dir ui_runs --out eval_set` — if
labels or runs changed; rebuilding changes case counts.)

## Full report

```
Cases: 113 total (4 corrections, 14 ground-truth events, 95 label-CSV events)

== IN/OUT vs human calls (judge on corrected ball) ==
Accuracy: 2/2 = 100.0%
  judge IN / human IN: 2

Drift: 0/3 - judge output unchanged since labeling.

== Hit-type classification ==
Accuracy: 2/4 = 50.0%
  detector racket / human floor: 1   <-- wrong
  detector wall / human none: 1   <-- wrong
  detector wall / human wall: 2
Detector false-positive hits (human: not a hit): 1

== Ball-position error (predicted vs corrected, px) ==
n=3  mean 9.6  median 0.0  p90 28.7  max 28.7 (corr:1784583924415:48)
  detected_center: n=2 mean 14.3px
  unknown: n=1 mean 0.0px

== Bounce timing ==
Detected frame confirmed as the bounce: 3/3

== Missed bounces (labeled, no detected hit at build time) ==
Missed: 71/109
  floor: 3
  racket: 2
  side_wall: 1
  wall: 65
Matched-pair type agreement: 38/38
```
