av
flask
# Pinned to what this project is actually developed against. Unpinned, pip on
# Ubuntu's python 3.10 resolves inference 0.14.0 (the Mac's 3.12 gets 1.3.5),
# and 0.14 calls sv.BoundingBoxAnnotator, which supervision 0.29 removed — so
# the rfdetr ball backend dies at frame 0 on a Lambda box while working fine
# locally. supervision is pinned with it because that pair is the constraint.
inference-sdk==1.3.5
inference==1.3.5
supervision==0.29.1
numpy
opencv-python
pandas
Pillow
python-dotenv
pytest
scikit-learn
torch

# hardware/ STL generation (hardware/generate_mount_stls.py)
trimesh
manifold3d
shapely

# docs/*.pdf builds (docs/build_spec_pdf.py, docs/build_annotation_pdf.py).
# The mount spec additionally needs rsvg-convert on PATH for its figure pages;
# the annotation guide is text-only and needs neither.
reportlab
pypdf

# Roboflow dataset pull (train_yolo_ball.py). Lazy-imported, so the test suite
# and prepare_ball_dataset.py — which reads an already-exported COCO tree — both
# run without it.
roboflow

# Person detection (player attribution). Apache-2.0. Lazy-imported in
# person_model.py only — the test suite must run without it.
rfdetr==1.8.3
