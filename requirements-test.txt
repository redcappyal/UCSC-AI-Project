# Test-only dependency set.
#
# The full requirements.txt pulls `inference` (and with it torch), which is
# gigabytes and minutes of CI time. inference_engine.py imports that stack
# lazily, so the whole test suite runs without it — verified, 348 passing.
# Keep this list minimal; if a test starts needing the real model runtime,
# that test belongs behind a marker, not in the default CI job.
flask
numpy
# Court-line Hough thresholds are validated against 4.10. Later releases
# change both HoughLinesP's wrapper shape and which marginal segments it
# returns, making the synthetic detector fixtures nondeterministic.
opencv-python-headless==4.10.0.84
pandas
python-dotenv
pytest
scikit-learn
